# چک‌لیست پیاده‌سازی و استقرار مینی‌اپ تلگرام CODM Attachments

این چک‌لیست، مسیر اجرایی پیاده‌سازی مینی‌اپ را به چند فاز تقسیم می‌کند تا بتوان مراحل را به‌صورت گام‌به‌گام دنبال و پیگیری کرد.

---

## فاز ۱: طراحی و آماده‌سازی (۱–۲ روز)

- [ ] مطالعه کامل اسناد زیر:
  - [ ] `README.md`
  - [ ] `ARCHITECTURE.md`
  - [ ] `IMPLEMENTATION.md`
  - [ ] `API_SPEC.md`
  - [ ] `SECURITY.md`
- [ ] تأیید معماری کلی (Bot + MiniApp + API + DB).
- [ ] تصمیم‌گیری درباره فناوری API (FastAPI / Flask / دیگر).
- [ ] تصمیم‌گیری درباره فناوری Frontend (React / Vue / HTML ساده).

---

## فاز ۲: زیرساخت Backend (۲–۳ روز)

- [ ] اضافه کردن تنظیمات مینی‌اپ به `.env` و `config` (مانند `MINIAPP_ENABLED`, `MINIAPP_URL`).
- [ ] ایجاد ماژول API (مثلاً `miniapp_api.py`) با ساختار اولیه:
  - [ ] endpoint سلامت (`GET /miniapp/v1/health`).
  - [ ] endpoint احراز هویت (`POST /miniapp/v1/auth/verify`).
- [ ] اتصال API به `DatabaseAdapter` و `AttachmentAnalytics`.
- [ ] پیاده‌سازی اولیه endpoint های read-only:
  - [ ] `GET /miniapp/v1/categories`
  - [ ] `GET /miniapp/v1/weapons`
  - [ ] `GET /miniapp/v1/attachments`
  - [ ] `GET /miniapp/v1/trending`
  - [ ] `GET /miniapp/v1/search`
- [ ] پیاده‌سازی `POST /miniapp/v1/events` برای ثبت view/click/rate.

---

## فاز ۳: یکپارچه‌سازی با ربات (۱–۲ روز)

- [ ] اضافه کردن دکمه «📱 مینی‌اپ» به منوی اصلی در `MainMenuHandler.start`.
- [ ] اتصال دکمه به `WebAppInfo(url=MINIAPP_URL)`.
- [ ] تنظیم BotFather (در صورت نیاز) برای Web App.
- [ ] تست دستی:
  - [ ] /start → نمایش دکمه مینی‌اپ.
  - [ ] باز شدن Web App بدون خطای TLS.

---

## فاز ۴: Frontend مینی‌اپ (۳–۵ روز)

- [ ] ایجاد ساختار پروژه Frontend (React/Vue/... ).
- [ ] پیاده‌سازی صفحات اصلی:
  - [ ] صفحه دسته‌ها (Categories).
  - [ ] صفحه سلاح‌ها (Weapons).
  - [ ] صفحه اتچمنت‌ها (Top/All/Season Top).
  - [ ] صفحه ترندها (Trending).
- [ ] اتصال به API:
  - [ ] فراخوانی `/auth/verify` و مدیریت session.
  - [ ] فراخوانی endpoint های لیست و نمایش داده.
  - [ ] ارسال رویدادها به `/events`.

---

## فاز ۵: امنیت، تست و بهینه‌سازی (۲–۳ روز)

- [ ] پیاده‌سازی و تست کامل اعتبارسنجی `initData` مطابق `SECURITY.md`.
- [ ] افزودن rate limiting روی endpoint های حساس.
- [ ] نوشتن تست‌های واحد/یکپارچه برای API (با pytest).
- [ ] اجرای سناریوهای E2E طبق `TESTING_VALIDATION.md`:
  - [ ] سناریو باز شدن مینی‌اپ.
  - [ ] مشاهده سلاح‌ها و اتچمنت‌ها.
  - [ ] ترندها و جستجو.
  - [ ] رویدادهای view/click/rate.
- [ ] اندازه‌گیری زمان پاسخ API و بهینه‌سازی در صورت نیاز.

---

## فاز ۶: استقرار Production و مانیتورینگ (۲–۳ روز)

- [ ] آماده‌سازی سرور Production (یا Docker/Compose) مطابق `DEPLOYMENT_GUIDE.md`.
- [ ] تنظیم Webhook (در صورت استفاده) روی دامنه‌ی HTTPS.
- [ ] استقرار Frontend مینی‌اپ روی همان دامنه.
- [ ] آخرین تست دستی روی محیط واقعی:
  - [ ] /start و باز شدن مینی‌اپ.
  - [ ] عملکرد کامل مسیرهای اصلی.
- [ ] راه‌اندازی مانیتورینگ:
  - [ ] لاگ‌های ربات و API.
  - [ ] (اختیاری) اتصال به Sentry/Prometheus.

---

## وضعیت نهایی

پس از تیک خوردن تمام موارد بالا:

- مینی‌اپ تلگرام بر روی زیرساخت فعلی ربات به‌طور کامل فعال است.
- تمام مسیرهای اصلی استفاده (مرور، جستجو، ترندها، پیشنهادها) از طریق UI گرافیکی مینی‌اپ قابل دسترس هستند.
- API و لایه امنیتی مطابق اسناد `API_SPEC.md` و `SECURITY.md` پیاده‌سازی شده‌اند.

این چک‌لیست می‌تواند در طول توسعه به‌عنوان مرجع اصلی پیشرفت (Progress Tracker) استفاده شود.
