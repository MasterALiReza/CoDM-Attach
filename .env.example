# ============================================================================
# CODM Bot - Configuration Example
# ============================================================================
# این فایل نمونه‌ای از تنظیمات است
# برای استفاده:
#   1. این فایل را با نام .env کپی کنید
#   2. مقادیر YOUR_*_HERE را با مقادیر واقعی جایگزین کنید
# ============================================================================

# ----------------------------------------------------------------------------
# Telegram Bot Configuration (ضروری) ⚠️
# ----------------------------------------------------------------------------
# توکن ربات از @BotFather
BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# آیدی عددی ادمین اصلی (از @userinfobot)
SUPER_ADMIN_ID=YOUR_TELEGRAM_USER_ID_HERE

# نام کاربری ربات (اختیاری)
BOT_USERNAME=codm_attachments_bot

# کانال پشتیبانی (اختیاری)
SUPPORT_CHANNEL=@your_channel

# ----------------------------------------------------------------------------
# Database Backend (ضروری) ⚠️
# ----------------------------------------------------------------------------
# Options: 'postgres' only
DATABASE_BACKEND=postgres

# ----------------------------------------------------------------------------
# PostgreSQL Connection (برای production) 🚀
# ----------------------------------------------------------------------------
DATABASE_URL=postgresql://codm_admin:your_password@localhost:5432/codm_bot

DB_HOST=localhost
DB_PORT=5432
DB_NAME=codm_bot
DB_USER=codm_admin
DB_PASSWORD=your_secure_password

# ----------------------------------------------------------------------------
# Connection Pool
# ----------------------------------------------------------------------------
DB_POOL_SIZE=20
DB_POOL_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_POOL_PRE_PING=true

# ----------------------------------------------------------------------------
# Full-Text Search
# ----------------------------------------------------------------------------
PG_TRGM_THRESHOLD=0.3
FTS_MIN_QUERY_LENGTH=2
FTS_MAX_RESULTS=50

# ----------------------------------------------------------------------------
# Cache Settings
# ----------------------------------------------------------------------------
CACHE_ENABLED=true
CACHE_TTL_CATEGORIES=3600
CACHE_TTL_WEAPONS=1800
CACHE_TTL_ATTACHMENTS=300
CACHE_TTL_USERS=600
CACHE_MAX_SIZE=1000

# ----------------------------------------------------------------------------
# Performance
# ----------------------------------------------------------------------------
LOG_SLOW_QUERIES=true
SLOW_QUERY_THRESHOLD=100
ENABLE_QUERY_CACHE=true
BATCH_SIZE=50

# ----------------------------------------------------------------------------
# Logging
# ----------------------------------------------------------------------------
DB_LOG_LEVEL=INFO
LOG_DB_QUERIES=false
DB_LOG_FILE=logs/database.log

# ----------------------------------------------------------------------------
# Security
# ----------------------------------------------------------------------------
DB_SSL_MODE=prefer
DB_MAX_RETRIES=3
DB_RETRY_DELAY=1

# ----------------------------------------------------------------------------
# Monitoring
# ----------------------------------------------------------------------------
ENABLE_HEALTH_CHECKS=true
HEALTH_CHECK_INTERVAL=60
ENABLE_PERFORMANCE_MONITORING=true

# ----------------------------------------------------------------------------
# Environment
# ----------------------------------------------------------------------------
ENVIRONMENT=production
DEBUG_MODE=false

# ----------------------------------------------------------------------------
# i18n Settings
# ----------------------------------------------------------------------------
DEFAULT_LANG=fa
SUPPORTED_LANGS=fa,en
FALLBACK_LANG=en
LANGUAGE_ONBOARDING=true

# ============================================================================
# نکات مهم:
# 1. BOT_TOKEN و SUPER_ADMIN_ID را حتماً تنظیم کنید
# 2. برای PostgreSQL، DATABASE_URL را با اطلاعات خود جایگزین کنید
# 3. برای امنیت، هرگز این فایل را commit نکنید
# ============================================================================
